# TODO

## 🔥 В работе
(пусто)

## 📋 Запланировано (следующая задача — первая в списке)
### Фаза 0 — Окружение
- [ ] Зависимости + docker-compose с Postgres + config.py
### Фаза 1 — Данные + схема БД ✅ ГОТОВО
- [x] Модели: users, teams, groups, players, group_matches, bracket_slots, predictions_*
- [x] Первая Alembic-миграция
- [x] Сид: 48 команд, 12 групп, расписание, структура сетки
- [x] Сид: база игроков — все 48 заявок (1245 игроков, позиции + даты рождения)
- [ ] (хвост) дозагрузить по 26-му игроку Канаде/Австрии/Иордании
### Фаза 2 — Бот + группы ✅ ГОТОВО
- [x] Каркас aiogram (приветствие, «Начать прогноз», FSM, DB-middleware)
- [x] UX ввода счёта групповых матчей (прогресс N/72, редактирование предыдущего)
- [x] Сервис расчёта таблиц групп + тесты (тай-брейки: очки→разница→голы→личные встречи)
- [x] Показ таблиц всех групп с пометкой выходящих
- [x] Смок-тест: getMe @fifacup2026_bot, 13 тестов, бот запущен на SQLite
### Фаза 3 — Плей-офф ✅ ГОТОВО
- [x] Отбор 8 лучших третьих мест + назначение по слотам R32 (паросочетание, все 495 комбинаций)
- [x] Построение R32 из мест в группах
- [x] Продвижение по сетке (1/16 → … → матч за 3-е → финал), резолвер победителей/проигравших
- [x] UX прогноза плей-офф + ничья → выбор прошедшего (пенальти)
- [x] Итог: чемпион / финалист / 3-е место
- [x] Тесты: 19/19 (вкл. полную интеграцию сетки до финала)
- [ ] (хвост) подложить офиц. таблицу FIFA для точной привязки 3-х мест
### Фаза 4 — Награды + символическая сборная ✅ ГОТОВО
- [x] UX выбора наград drill-down «команда → игрок» с пагинацией
- [x] Фильтры: вратарь (Золотая перчатка), возраст (молодой игрок), бомбардир + число голов
- [x] Команда-сенсация / разочарование (выбор команды)
- [x] Конструктор сборной 4-3-3 по позициям, без повторов игроков
- [x] Итоговая сводка: чемпион/призёры (из сетки) + награды + сборная
- [x] Тесты: 23/23
### Фаза 5 — PDF ✅ ГОТОВО
- [x] Выбрана либа ReportLab (локально, без нативных зависимостей, кириллица через TTF)
- [x] Сбор данных прогноза (report_data) + рендер: подиум, таблицы групп (2 кол.),
      третьи места, сетка по раундам, награды, символическая сборная
- [x] Кнопка «Скачать PDF» + отправка файла WM2026_Prediction_<user>.pdf
- [x] Тесты (25/25) + визуальная проверка реального PDF — кириллица и вёрстка ОК
### Фаза 6 — Полировка + локальный деплой ✅ ГОТОВО
- [x] Качественный долг: ruff чистый (StrEnum, импорты, E501, zip strict); миграции исключены из линта
- [x] Харденинг edge-cases: глобальный error-handler (dp.errors), guard'ы устаревших
      callback'ов (accessible_message), единый парсер счёта (services/scores) для групп и плей-офф
- [x] Защита точек входа плей-офф/награды/PDF от пустых/неполных состояний
- [x] README с инструкцией запуска (Docker+Postgres и локально на SQLite)
- [x] Финальная проверка: 56/56 тестов, ruff чистый
### Фаза 7 — Безопасность + анти-флуд + деплой VPS ✅ ГОТОВО
- [x] Безопасность: `.dockerignore` (секреты не попадают в образ), non-root USER в Dockerfile,
      escape username в PDF (ReportLab), лимиты ресурсов контейнера
- [x] Анти-флуд: throttling-middleware (транспортный, до сессии БД) + доменный кулдаун
      «3 попытки → пауза 30 сек» (services/antiflood) в группах/плей-офф/наградах
- [x] Catch-all фоллбэк на мусорный текст (handlers/fallback)
- [x] docs/DEPLOY.md: изоляция от основного проекта (отдельная БД+юзер least-privilege,
      name/container_name, chmod 600 .env, бэкап только своей базы)
- [x] Тесты: 65/65, ruff чистый
- [x] Проверен docker build на этом VPS: пофикшен баг `pip install -e .` до `COPY src`
      (Dockerfile), сборка/миграции/сид проходят. Postgres хост-порт 5433 (5432 занят).
- [ ] (на деплое) выбрать сетевой вариант A/B для доступа к Postgres (docs/DEPLOY.md)
### Фаза 8 — Просмотр своего прогноза ✅ ГОТОВО
- [x] Раздел «📋 Мои прогнозы»: команда `/my` + кнопка (меню /start и после групп).
      Меню «⚽ Группы» / «🏆 Плей-офф» показывает спрогнозированные счета матчей
      (read-only, без сравнения с реальными результатами — это отдельная post-MVP фича).
- [x] `repo.get_group_predictions` (счета по группам с именами команд);
      плей-офф переиспользует `get_bracket_preds` + `get_teams_map`;
      общий маппинг стадий вынесен в `services/bracket` (STAGE_ORDER/STAGE_BY_NUM).
- [x] Длинные ответы дробятся по лимиту Telegram; тесты 73/73, ruff чистый по новым файлам.
### Фаза 9 — Точность прогнозов (группы) + рейтинг ✅ ГОТОВО
- [x] Таблица `actual_results` (факт. счета, ключ match_number) + Alembic-миграция;
      сервис `accuracy` (угадано исходов/точных счетов, проценты, рейтинг одним SQL).
- [x] Показ: `/score` + кнопка «🎯 Моя точность» в меню «Мои прогнозы», `/top` (рейтинг + место).
- [x] Ввод результатов (только админ, `/results`): автопоиск Tavily→Groq с подтверждением +
      ручной ввод по матчам кнопками; дневной фоновый автопоиск шлёт админу черновик.
      Автосохранение запрещено — веб-данные подтверждает админ (барьер `result_matching`).
- [x] Тесты 85/85, ruff чистый по новым файлам; миграция применяется (проверено).
- [ ] (на деплое) задать `GROQ_API_KEY`, `TAVILY_API_KEY` в `.env`; сверить актуальный `GROQ_MODEL`.

## 🔒 Безопасность — долги (не горит)
- [x] 2026-06-13: закрыт порт Postgres — был `0.0.0.0:5433` (доступен из интернета),
      переведён на `127.0.0.1` (коммит `ops: закрыть порт Postgres на loopback`).
      Контекст: роль `fifa26` — суперюзер, дефолт-пароль + открытый порт = потенциальный
      `COPY FROM PROGRAM` → RCE в контейнере БД на общем с `batumi` хосте.
- [ ] **Сменить дефолтный пароль БД `change-me`** (defense-in-depth; порт уже закрыт, не горит).
      Шаги: сгенерить стойкий пароль → `ALTER ROLE fifa26 WITH PASSWORD '…'` в живой БД →
      обновить `.env` (`POSTGRES_PASSWORD` + `DATABASE_URL`) → пересоздать только контейнер
      бота (`docker compose up -d bot`) → проверить, что бот видит БД. `.env` в git не идёт.
- [ ] (опц.) Харденинг соседнего `batumi`: netdata торчит наружу под basic-auth с bcrypt
      cost=5 (поднять cost или закрыть по IP/VPN); убрать неиспользуемый
      `/etc/traefik/dynamic.yml.bak`. Аудит 2026-06-13 — дыр нет, это косметика.

## 🎮 Игра «Пенальти ЧМ-2026» (отдельный сайт, план согласован 2026-06-10)
План и решения: [docs/penalty-game-plan.md](docs/penalty-game-plan.md)
- [ ] Фаза 1 — скелет: game/frontend (Vite+Phaser, сцена с воротами),
      game/api (FastAPI, GET /api/teams из БД бота), nginx + compose
- [ ] Фаза 2 — ядро геймплея: прицел+сила, полёт мяча, ИИ-вратарь,
      серия из 10 ударов, счёт/комбо
- [ ] Фаза 3 — сборные + серверный расчёт ударов (анти-чит), game-сессии
- [ ] Фаза 4 — лидерборд: миграции game_players/game_sessions, ник, топ all/week
- [ ] Фаза 5 — полировка: мобильная адаптация, шаринг, HTTPS-деплой

## 🧊 Post-MVP (после турнира, зависят от реальных результатов)
- [ ] Очки за точность, сравнение прогнозов, публичная ссылка, шаринг
- [x] **Группы: точность (исходы/точные счета) + рейтинг** — сделано в Фазе 9
      (`actual_results`, `services/accuracy`, `/score`, `/top`, ввод `/results` Tavily/Groq).
- [ ] **Статистика угадывания — остаток (награды + чемпион/призёры в PDF/очках):**
      нужны фактические награды/сборная и единая система очков.
  - Метрики: чемпион/призёры, награды, символическая сборная.
  - Where: расширить `services/accuracy` + добавить раздел в PDF и систему очков
    (точный счёт vs только исход — разные веса).

## ✅ Готово
- [x] **Сводка результатов — время по Берлину 11:00** (DIGEST_HOUR=11; срез 06:00).
- [x] **Авто-сводка сетки плей-офф (после каждой сыгранной стадии, 11:00):**
  - Реальная сетка строится из результатов: места в группах → R32 → победители (учёт
    пенальти через `score.winner` API) → следующие стадии (`services/playoff_actual`).
  - Сообщение игроку по стадии: угаданные прошедшие (✅), «кто вышел вместо твоих» (🔄),
    счётчики угаданных прошедших и совпавших пар (`services/playoff_digest`).
  - Без миграций: `ActualResult` (73..104) — журнал «разослано» (digested);
    `count_actual_results` сужен до матчей ≤72. Подключено в `digest_loop` (11:00).
  - Тесты: парсер победителя (пенальти), матчинг API по парам, рендер сводки,
    выбор готовых стадий. Итого 105/105.
- [x] Подготовка к запуску на большую аудиторию (безопасный путь):
  - [x] Лимиты CPU/RAM/pids для bot (512m/1.0) и db (768m/1.0) — защита соседнего
        боевого проекта `batumi` на том же VPS.
  - [x] Бэкап БД: scripts/backup.sh (pg_dump только своей базы, gz, ретеншн 14 дней),
        проверен; нужна cron-строка на хосте (ниже).
  - [x] Нагрузочный тест scripts/loadtest.py: 300 конкурентных юзеров = 21.6k записей
        за 18с (~17 юзер/с), Postgres не нагружен, bas/batumi не задет. Узкое место — 1 CPU бота.
  - [x] Инцидент: loadtest снёс реальных юзеров (id-диапазон пересёкся) → восстановили из
        бэкапа; фикс — отрицательные telegram_id для фейков. См. LESSONS.
  - [ ] (за тобой) поставить cron на бэкап; провести МЯГКИЙ запуск (друзья/малый канал),
        понаблюдать CPU бота и Telegram-429, и только потом пост на 700k.
- [x] Бот запущен на этом VPS (Docker): @fifacup2026_bot live, БД засеяна, polling идёт.
- [x] Админ-команда `/stats` (скрытая, по ADMIN_IDS): обзор «всего/завершили/кто заходил»
      + `/stats <id>` детали (призёры/награды). Сервис src/services/stats.py, тесты 3/3.
      Заодно: добавлен `aiosqlite` в dev-зависимости (тесты async-SQLite). Итого 68/68.
- [x] Создана базовая структура проекта (CLAUDE.md, агенты, скилы, рабочие файлы)
- [x] Согласован полный объём и план по фазам
- [x] Фаза 0: скелет (pyproject, config, Dockerfile, docker-compose, db base/session)
- [x] Фаза 1: модели БД + Alembic + сид-инфраструктура
- [x] Данные турнира: 12 групп, 48 команд, расписание, структура сетки
- [x] Игроки: Группы A, B, C (12 из 48 команд)
- [x] Тесты схемы и данных на SQLite — 7/7 проходят
- [x] Фаза 1 проверена end-to-end на SQLite: миграция + сид (1245 игроков, 8 слотов 3-х мест)
- [x] Собраны все 48 заявок ЧМ-2026 (русские имена, позиции, даты рождения)
