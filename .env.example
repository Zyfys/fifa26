# === Telegram ===
BOT_TOKEN=your-telegram-bot-token-here

# === PostgreSQL ===
POSTGRES_USER=fifa26
POSTGRES_PASSWORD=change-me
POSTGRES_DB=fifa26
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
# Полный async DSN (используется приложением); собирается из переменных выше
DATABASE_URL=postgresql+asyncpg://fifa26:change-me@localhost:5432/fifa26

# === App ===
LOG_LEVEL=INFO
# Telegram-ID админов через запятую (доступ к /stats и /results). Пусто = ни у кого.
ADMIN_IDS=

# === Реальные результаты (точность прогнозов) ===
# Groq — извлечение результатов из текста (бесплатный ключ: console.groq.com).
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
# Tavily — веб-поиск результатов матчей (бесплатный лимит: tavily.com).
TAVILY_API_KEY=
# Час суток (0–23) для дневного автопоиска результатов. Автопоиск работает,
# только если заданы оба ключа (Groq+Tavily) и есть хотя бы один ADMIN_IDS.
RESULTS_FETCH_HOUR=23
