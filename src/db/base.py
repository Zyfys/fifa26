"""Декларативный базовый класс моделей (без подключения к БД).

Отделён от движка/сессий (см. src/db/session.py), чтобы модели можно было
импортировать и тестировать без реального соединения с PostgreSQL.
"""

from sqlalchemy.orm import DeclarativeBase


class Base(DeclarativeBase):
    """Общий декларативный базовый класс для всех моделей."""
